
TYPE
	State_type : 
		(
		CutterMoveBackward,
		CutterCompleted,
		CutterMoveForward,
		ConveyorStopping,
		ConveyorMoving,
		ConveyorStarting,
		AxisEnabled,
		InitializeAxis,
		CutterBackward,
		CutterForward,
		ConveyorHolding,
		WaitProduct,
		Initialize,
		Init,
		StopState,
		HaltState,
		WaitForConveyor,
		CheckReset,
		RegulatePosition
		);
END_TYPE
